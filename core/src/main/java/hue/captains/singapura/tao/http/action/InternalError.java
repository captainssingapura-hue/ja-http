package hue.captains.singapura.tao.http.action;

/**
 * Marker interface for error details intended for internal tracking.
 * Implementations are logged server-side and never exposed to external callers.
 */
public interface InternalError {}
